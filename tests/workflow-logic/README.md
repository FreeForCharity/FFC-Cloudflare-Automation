# Workflow-logic tests

Unit tests for the **logic embedded inside GitHub Actions workflows** — the `github-script` blocks,
bash steps, and pwsh steps where this repo's real automation decisions live. The repo's other guards
(reference guard, safety-doc consistency, catalog freshness, actionlint) validate workflow
_structure_; these validate workflow _behavior_, so regressions fail a PR instead of surfacing as
the next quiet production failure.

## How it works

- `wf_extract.py` pulls the **actual script text out of the workflow YAML** at test time. Tests can
  never drift from what ships: change the embedded logic and the tests run against your change
  immediately.
- `harness/github_script_shim.mjs` executes a `github-script` body the same way
  `actions/github-script` does (async wrapper with `context`/`core`/ `github`), capturing
  `setOutput`/`setFailed`/`notice` for assertions.
- `harness/gh` is a fake `gh` CLI put on `PATH`. Scenario fixtures are fed through `TEST_*` env vars
  and every invocation is logged so tests can assert which API calls were — or were not — made.

## Covered today

| Module                                      | Workflow under test                                               | What it locks down                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test_701_parse.py`                         | 701 website-provision `resolve` script                            | all three event paths (manual, `repository_dispatch` client_payload, issues), domain normalization, loud failure on missing domain, no comment-posting for non-issue runs (refs #419)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `test_726_drift_audit.py`                   | 726 drift audit bash                                              | **PUBLIC/private visibility-enum regression** (the bug that silenced `no-merge-queue` in #667), paginated teams fetch, distinct `teams-check-failed` vs false `missing-teams`, Option C private-repo checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `test_720_owner_parse.py`                   | 720 create-repo tier-team grants (pwsh)                           | owner-qualified `RepoName` parsing, foreign-owner skip makes zero API calls, all five tier teams granted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `test_736_preflight.py`                     | 736 archive `preflight` job                                       | live archive requires a matching successful dry-run within 48h; fails fast on missing/already-archived; warns (non-blocking) on recent push/Pages/open-issue references; dry runs bypass all checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `test_502_deliver.py`                       | 502 GA-report `deliver` bash                                      | **shallow-clone sync-branch regression (#733):** bases the `chore/ga-data-sync` checkout on `FETCH_HEAD` (a `--depth 1` clone has no `origin/<branch>` ref); fresh-branch + `pr create` when absent; early-exit with no push/PR churn when data is unchanged. Git runs against a local bare repo, `gh` is faked — no network.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `test_734_stale_run_janitor.py`             | 734 stale-waiting-run janitor `github-script`                     | the only monitor that **cancels** runs: staleness cutoff honours `maxAgeDays` (a huge threshold cancels nothing); only runs older than the cutoff are cancelled; dry-run reports without cancelling; a scheduled run defaults to a real cancel; pagination fetches every page; a per-run cancel 409 is swallowed so the sweep stays green; cancel-only — never approves a gate. Uses `harness/actions_run_shim.mjs` (Actions runs API + `core.summary` mocks).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `test_733_credential_rotation_reminders.py` | 733 quarterly credential-rotation reminders `github-script`       | the **quarterly seed-one-per-family** monitor: no marker opens exactly one `security`-labelled issue per credential family (marker + runbook + Key Vault refs + "rotation = new KV secret version"); an existing per-family+quarter marker skips that family (no duplicate); all four present is a clean no-op; the dedupe lookup keys on the `<!-- rotation-<family>-<Q> -->` marker via a `repo:… in:body "<marker>"` search; a **failed** search falls through the script's `.catch` to create rather than silently skip; the quarter label derives from a frozen UTC clock; issues-write/contents-read only, never touches a gate. Reuses `harness/issues_api_shim.mjs` (adds a `search.issuesAndPullRequests` mock + `TEST_NOW_MS` clock freeze).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `test_724_initialize_labels.py`             | 724 Initialize Labels `github-script`                             | the **label upsert loop** that seeds `.github/labels.yml` (the labels the Agentic OS keys on): an existing label is updated, never re-created; a missing label (getLabel 404) is created; every configured label is processed; a **non-404** getLabel error is logged and that label skipped while the rest of the sweep still runs (never aborts); a create/update failure is swallowed the same way; the config is read from `.github/labels.yml`; the job's scope is read-the-repo + write-labels (`contents: read` for checkout, `issues: write` + `pull-requests: write`) with no `environment` gate, so it can never advance a deployment gate. Uses `harness/labels_api_shim.mjs` (labels API + a `require` shim injecting the parsed config, since the plain-node CI harness has no `js-yaml`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `test_731_actions_run_metrics.py`           | 731 Actions-run-metrics (30d per-workflow stats) `github-script`  | the **read-only aggregation** monitor that feeds the ffcadmin metrics dashboard: per-workflow grouping keyed on `name` (falling back to `path`); success/failure/cancelled bucketing (other conclusions count as runs only); `successRate`/`avgDurationSec` rounding with negative durations clamped to 0 and timestamp-less runs contributing no duration; output sorted by run count desc; the 30-day window passed as the `created:>=` filter from a frozen clock; pagination fetches every page and stops on a short one; the 2000-run safety cap bounds the sweep; the artifact is written as valid JSON with the expected envelope; contents/actions-read only, never touches a gate. Uses `harness/metrics_fs_shim.mjs` (runs list mock + a capturing `require('fs')` + `TEST_NOW_MS` clock freeze).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `test_740_scheduled_failure_alert.py`       | 740 scheduled-workflow rolling failure alert `github-script`      | the hub's **single failure alerter**, and the guards specific to its being a **poll** rather than a `workflow_run` watcher (#843 — that event has never fired in this repo): the watch list resolves **past `per_page`** (>100 workflows; an unpaginated lookup silently drops the tail), the marker keys on the **workflow** name and never `run.name` (which carries the rendered `run-name:` — 726/735 set one), a watched name matching no workflow **fails the run loudly** rather than going unwatched, and an unreadable run list warns instead of reading as green. Plus the rolling-issue state machine inherited from retired 732: one issue per watched workflow (a green run of one never closes another's), `cancelled`/`timed_out` are reportable, a **declined approval gate is not an outage** (no job `failure` + ≥1 `cancelled`) while an unreadable job list still alerts, default-branch runs only, and the sweep spends at most one `issues.listForRepo` (zero when every watched run is benign). Also asserts no `workflow_run` trigger survives anywhere in the repo. Uses `harness/issues_api_shim.mjs` (issue mocks + paged `listRepoWorkflows`/`listWorkflowRuns` and a modelled `github.paginate`).                                                                                                                                                                                                                                                                     |
| `test_120_cutover_gh_errors.py`             | 120 bulk-cutover bind / dispatch / poll bash                      | the four **L02 error-swallowing** sites (#889): an unreadable Pages cert read reports `unknown` and never a 403 body, and never triggers the cname→null→cname re-bind on a guess (while a genuinely `none` state still does); an unreadable `gh run list` records `NO_RUN_ID` instead of carrying the error body forward **as a run id** (which made the poll step report a smoke FAILURE for a healthy run); an unreadable `gh run view` reports `error:null`. `sleep` is shimmed per-test, not in `harness/`, so the real 10s settles don't slow the suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `test_729_add_collaborator.py`              | 729 add-collaborator grant bash                                   | the role **read-back** after a successful grant (#889): an unreadable `/permission` read falls back to the requested permission with a warning, instead of printing the API's error body into the log and job summary as the role the user now holds; a readable role is reported as read (the two can differ, which is why the read exists); an invitation (201) skips the read entirely; a failed grant reports as failed; a dry run issues no PUT.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `test_730_environment_gate_audit.py`        | 730 environment-gate audit bash                                   | the **pipeline** variant of L02 (#889): a 403 used to flow through `\| sort` into `$names`, so the loop iterated over an API error message as environment names — and an unreadable audit rendered as a table row rather than a failure. Now an unreadable list fails the step (after publishing the summary) and can never read as "no gates"; a genuinely empty list is a clean pass; gated environments render with reviewers, sorted; a single unreadable environment is marked, never assumed open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `test_dry_run_skips_write_gates.py`         | 701 job-level `dry_run` + the write-credential class (structural) | **a dry run must not mint a write credential or park on a write gate** (#983): `dry_run` was enforced per-STEP, so the job started, the run parked on a reviewer gate and a writer credential was pulled from Key Vault before any step decided to do nothing. Derives the job list by **parsing every workflow** (write-scoped `*-from-kv` action, `scope:` omitted counting as write, or a `wr-all-*` Key Vault name) rather than a hard-coded list — the real population is 27 jobs across 23 workflows, enumerated as debt so the number is visible. Enforces that a skip is only legitimate when an **ungated job still runs the same script** (701's `dns_preview` relocates the rehearsal to `cloudflare-prod-read`; a zone-EXISTENCE probe like `zone_check` does not qualify), and conversely that **702 and 736 are never swept into a skip** — 702's dry run IS the clone+build and defaults to true, and 736's dry run is the evidence its own live preflight demands within 48h. Also pins that only skip-SHAPED conditions count (a bare `if: inputs.dry_run` inverts the guard), and that `== false` is unsafe wherever the value is a **string** (`github.event.inputs.*`, or a non-boolean input type) because `"false"` casts to NaN — not, as first written, because a null `inputs` context makes it false (Null and `false` both cast to `0`, so `null == false` is TRUE). Non-vacuity is asserted directly — blinding the detector fails the module rather than emptying it. |

## Running

```bash
python3 tests/workflow-logic/run_all.py     # everything (what CI runs)
python3 tests/workflow-logic/test_701_parse.py   # one module
```

Needs `python3` + PyYAML and `node`; the 720 module needs `pwsh` and self-skips where PowerShell
isn't installed (it always runs in CI).

## Adding coverage

When you add or change embedded workflow logic, add a scenario here:

1. Extract the step with `wf_extract.step_run(...)` / `step_github_script(...)`.
2. Drive it with a simulated context (JS) or `TEST_*` fixtures + the fake `gh` (shell), asserting on
   outputs, flags, or the gh call log.
3. `run_all.py` auto-discovers any `test_*.py` module — no registration.
4. **Give the module a runner.** `run_all.py` executes each module as a plain script
   (`python3 test_x.py`), so a module without an `if __name__ == "__main__":` block defines its
   tests and executes none of them — and exits 0, which the harness reported as a pass. The modules
   do not share one reporting convention (most print `  PASS <name>` per test; `test_502` prints a
   single summary line), but a module that exits 0 **in silence** is now failed as having run
   nothing.
