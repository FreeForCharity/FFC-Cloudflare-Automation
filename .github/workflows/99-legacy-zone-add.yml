name: '99. Deprecated - Cloudflare Zone Add (removed)'

permissions:
  contents: read
  actions: read

on:
  workflow_dispatch:

jobs:
  add:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: |
          echo "This legacy workflow required a separate zone-create token and has been removed."
          echo "Use the supported DNS workflows that authenticate with CLOUDFLARE_API_KEY_DNS_ONLY."
          exit 1
